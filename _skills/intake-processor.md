---
name: opensynapse-intake-processor
version: 1.0.0
description: >
  사용자의 원시 입력을 받아 Obsidian 노트로 변환하는 처리 로직.
  pkm-core.md를 참조하여 분류, 템플릿 적용, 출력을 수행합니다.
---

# OpenSynapse — Intake Processor

## 처리 파이프라인

```
입력 → 타입 감지 → 신뢰도 평가 → [질문] → 분류 → 템플릿 → 출력
```

---

## Step 1: 노트 타입 감지

| 입력 신호 | 노트 타입 | 템플릿 |
|-----------|-----------|--------|
| "미팅", "회의", 참석자 이름, "논의했어" | meeting | `meeting` |
| URL, "읽었는데", "기사", "논문", 저자명 | literature | `literature-note` |
| "책", "읽은 책", 저자+제목 패턴 | book | `book-note` |
| "만들려고", "프로젝트", 목표+마감 | project | `project-index` |
| 단일 개념, "배웠는데", "알았는데" | permanent | `permanent-note` |
| "이 주제 정리해줘", 여러 노트를 묶는 허브 | moc | `moc` |
| 특정 사람 기록, "OOO랑 얘기했는데" | person | `person` |
| "이번 주 리뷰", "주간 정리" | weekly review | `weekly-review` |
| "이번 달 리뷰", "월간 정리" | monthly review | `monthly-review` |
| 짧은 할 일, "기억해야 할 것", 오늘 일어난 일 | daily 캡처 | `daily-note` |
| 분류 안 되는 모든 것 | fleeting → Inbox | `fleeting-note` |

---

## Step 2: 필수 필드 체크

**미팅 노트 필수:** 참석자, 목적, 액션 아이템 (하나라도)  
**프로젝트 필수:** 목표 (done이 어떤 모습인지), 마감  
**문헌/도서 노트 필수:** 핵심 인사이트 1개 이상  
**영구 노트 필수:** 핵심 아이디어 명확히  
**MOC 필수:** 주제(topic) 명확히  
**Person 필수:** 이름, 역할/맥락  

---

## Step 3: 질문 전략

**질문은 필수 필드가 빠진 경우에만.**  
**한 번에 최대 3개. 가능한 1-2개로.**  
**모호한 입력에는 딱 한 가지만 물어볼 것.**

형식:
```
[노트 타입 확인됨] 빠진 게 있어서 빠르게만요:
1. [질문]
2. [질문]
```

입력이 완전히 불명확하면:
```
조금 더 알려주세요 — 어떤 주제였나요?
```

---

## Step 4: 분류 (pkm-core.md 참조)

PARA 결정 트리 적용 → 저장 경로 결정  
03_Resources 하위 토픽 폴더는 pkm-core.md 또는 볼트에 정의된 대로 매칭.  
예: AI/ML → `AI-ML/`, 웹/앱 개발 → `Web-Dev/`, 생산성/습관 → `Personal-Growth/`, 글쓰기/책 → `Writing/` 등.  
새 토픽이 필요하면 새 폴더 제안할 것.

---

## Step 5: 출력

### Claude (파일 직접 생성 불가 — 텍스트 출력)

완성된 마크다운을 코드블록으로 출력하고 끝에 반드시:

```
---
📁 저장 위치: 경로/파일명.md
🔗 연관 노트: [[노트A]], [[노트B]]
⏭️ 다음 액션: 한 가지 제안
```

### Antigravity / Cursor (파일 직접 생성)

파일을 볼트 경로에 직접 생성하고:

```
✅ 생성 완료: 경로/파일명.md
🔗 연관 노트: [[노트A]], [[노트B]]
⏭️ 다음 액션: 한 가지 제안
```

---

## 복수 아이디어 처리

한 입력에 3개 이상 다른 주제가 섞이면:
1. "이 안에 N개의 노트가 보여요. 각각 만들어드릴게요." 안내
2. `--- 노트 1/N ---` 형식으로 순차 출력

---

## 언어 규칙

- 노트 본문 → 입력 언어 그대로
- frontmatter 키 → 영어 (YAML 표준)
- 파일 경로/파일명 → 영어 (kebab-case)
- 마지막 안내 (📁🔗⏭️) → 한국어

---

## 출력 품질 체크리스트

출력 전 확인:
- [ ] frontmatter 필수 필드 완성 (빈 필드 없음)
- [ ] 입력 언어로 본문 작성
- [ ] 태그 체계 준수 (type/ + context/ + topic/)
- [ ] [[링크]] 1개 이상 포함
- [ ] 파일 경로 명시
- [ ] 노트가 permanent이면 아이디어 하나만 (원자성)

---

## 엣지 케이스

**너무 모호한 입력** ("그거", "아무튼", "맞다") →  
"어떤 주제였나요? 한 문장으로도 충분해요."

**사용자가 "그냥 inbox에 넣어줘"** →  
분류 생략, fleeting-note 템플릿으로 `00_Inbox/`에 저장

**볼트에 없는 새 토픽** →  
노트 생성 + "`03_Resources/새토픽/` 폴더를 새로 만드는 게 좋을 것 같아요"

**다국어 혼용** →  
각 문장/단락의 주 언어를 따름. frontmatter는 항상 영어.

---

## 운영/관리 포인트 (효율)

아래는 네가 정의한 로직을 그대로 유지하면서, 실제 운용에서 “되돌리기/중복/일관성” 비용을 줄이기 위한 규칙입니다.

### 1) 신뢰도 점수(Confidence)와 질문 게이트

- 타입 감지 결과를 0~1로 내부 평가한다고 가정한다.
- \(confidence < 0.6\) 이면 분류/생성 전에 질문 1개만 한다.
- \(0.6 \le confidence < 0.8\) 이면 질문은 “필수 필드”에 한정해 1~2개만 한다.
- \(confidence \ge 0.8\) 이면 질문 없이 진행한다(단, 필수 필드가 명백히 비어 있으면 예외).

### 2) 아이디엠포턴시(중복 생성 방지)

- 같은 입력을 반복 처리해도 **결과가 크게 달라지지 않게** 한다.
- 이미 같은 날짜/같은 회의명/같은 URL의 노트가 있으면 “새 파일 생성”보다 “기존 파일에 추가(append)”를 우선 고려한다.

### 3) 파일 충돌(이름 겹침) 규칙

- 동일 경로에 동일 파일명이 있으면, 다음 중 하나를 선택한다:
  - (권장) 기존 파일에 “추가 기록”으로 append
  - 새 파일 생성 시 `-v2`, `-2` 같은 suffix를 붙여 충돌 회피

### 4) 템플릿 선택 우선순위

- 템플릿은 `pkm-core.md`의 링크(`06_Templates/`)를 단일 원본으로 삼는다.
- 타입이 확정되면 해당 템플릿을 그대로 사용하고, 필요한 최소 항목만 채운다.
- 타입이 애매하면 `fleeting-note`로 캡처 후 Inbox에 보낸다(= 되돌리기 비용 최소화).

### 5) 링크 추천(최소 1개) 전략

- 무조건 “많이”가 아니라, 아래 우선순위로 1~3개만 추천/연결한다:
  1. 프로젝트(있다면) `[[01_Projects/...]]`
  2. 주제 리소스 허브(있다면) `[[03_Resources/<주제>/...]]`
  3. 같은 소스(URL/책/논문) 기반 문헌 노트

### 6) 리뷰 큐 생성(운영 비용 절감)

- 질문이 남거나 분류가 애매했던 노트는 frontmatter에 `status/unprocessed` 같은 표식을 남겨,
  주간 리뷰에서 한 번에 처리할 수 있게 한다(특히 `00_Inbox/`).
